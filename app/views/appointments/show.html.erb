<h1> Appointment's Doctor </h1>

<p> <%= link_to(@appointment.doctor.name, doctor_path(@appointment.doctor.id)) %> </p>
<p> <%= link_to(@appointment.patient.name, patient_path(@appointment.patient.id)) %> </p>
